# 小松资产维护规范

- 此仓库是硅脑小松的公开资源目录，GitHub 保存文档、预览图片、公开元数据和下载链接；工程、模型、缓存、视频、代码包放 Cloudflare R2。
- 只发布用户明确置于本次发布范围的资产。不要自动扫描并上传全部旧项目，也不要把原始工作目录整包公开。
- 每项资产使用稳定英文 ID 和独立版本号，路径为 assets/<asset-id>/。保留中文标题。
- R2 对象键使用 assets/<asset-id>/<version>/<filename>，版本发布后不可覆盖；需要修改则新建版本。
- 下载链接只使用验证通过的公开 HTTPS 地址，不使用会过期的预签名链接。不把桶的管理端点或 API 密钥写入清单。
- 上传前检查密钥、账号、私人路径、聊天记录、日志和第三方权利。Blender 文件内的贴图路径、文本块和渲染输出路径也在检查范围内。
- 用户已于 2026-09-20 确认：原创模型、材质与 Blender 场景内容用 CC BY 4.0，原创代码用 MIT；第三方素材单独保留原许可。默认授权不代表有权再许可所有第三方素材。下载未发布或依赖未核验时 status 为 preview，downloads 为空。
- 公开下载前完成工程重开、依赖检查、文件大小和 SHA-256 核对；从公开地址取回文件验证后，才能将 status 改为 published。
- catalog.json 与资产 manifest.json、详情页的版本、状态和链接保持一致。README 不写本机绝对路径。
- 不创建周期性自动发布任务，不在仓库或日志中保存凭证。
